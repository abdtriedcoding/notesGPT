# React 19 is backward-compatible with React 18, but several dependencies
# (Radix UI, framer-motion, cmdk, sonner, etc.) still declare React 18-only
# peer ranges. They work fine at runtime, so we relax npm's strict peer check.
legacy-peer-deps=true
